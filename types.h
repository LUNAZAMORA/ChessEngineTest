#include <cassert>

enum Color{
    WHITE = 0, 
    BLACK = 1
};

/*
inline Color toggleColor(Color col){
    return col ^ BLACK;
}
*/

enum PieceType
{
   pt_pnil   = 0, // empty
   pt_wpawn  = 1,
   pt_bpawn  = 2,
   pt_knight = 3,
   pt_bishop = 4,
   pt_rook   = 5,
   pt_queen  = 6,
   pt_king   = 7,
};


enum Moves{
    
};